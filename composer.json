{
    "name": "yurderi/provisioner",
    "description": "A provisioner to configure apache hosts in a YAML file (supports SSL)",
    "type": "library",
    "license": "MIT",
    "minimum-stability": "stable",
    "require": {
        "symfony/console": "^4.2",
        "symfony/yaml": "^4.2",
        "twig/twig": "^2.6"
    },
    "autoload": {
        "psr-4": {
            "Yurderi\\Provisioner\\": "src/"
        }
    },
    "bin": [
        "bin/yp"
    ]
}